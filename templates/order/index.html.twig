{% extends 'base.html.twig' %}

{% block title %}Mes commandes - LEFA{% endblock %}

{% block body %}
<div class="container" style="max-width: 800px; margin: 50px auto; padding: 30px; background-color: #fff; border-radius: 10px; box-shadow: 0 0 15px rgba(0,0,0,0.1);">
    <h1>📦 Mes Commandes</h1>

    {% if orders is empty %}
        <p>Vous n'avez encore passé aucune commande.</p>
    {% else %}
        <table style="width: 100%; border-collapse: collapse; margin-top: 20px;">
            <thead style="background-color: #34495e; color: white;">
                <tr>
                    <th style="padding: 10px;">#</th>
                    <th style="padding: 10px;">Date</th>
                    <th style="padding: 10px;">Montant</th>
                    <th style="padding: 10px;">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for order in orders %}
                    <tr style="border-bottom: 1px solid #ddd;">
                        <td style="padding: 10px;">{{ order.id }}</td>
                        <td style="padding: 10px;">{{ order.createdAt|date('d/m/Y H:i') }}</td>
                        <td style="padding: 10px;">{{ order.total }} €</td>
                        <td style="padding: 10px;">
                            <a href="{{ path('order_show', { id: order.id }) }}" style="color: #2980b9;">Voir</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
</div>
{% endblock %}
